# AXIMA Znalostní Asistent — Python závislosti
# Instalace: pip install -r requirements.txt

# --- Web API ---
fastapi
uvicorn[standard]
pydantic

# --- HTTP klient (Ollama) ---
requests

# --- Vektorová databáze ---
qdrant-client>=1.11

# --- Sledování souborů / ingest ---
watchdog

# --- Čtení dokumentů ---
python-docx
openpyxl
pdfplumber

# --- Chunkování textu ---
langchain-text-splitters

# --- Přílohy: OCR screenshotů (vyžaduje systémový balík "tesseract-ocr" + "tesseract-ocr-ces") ---
pytesseract
Pillow
python-multipart   # nutné pro upload příloh (FastAPI UploadFile)

# --- SQL Server 2019 (roadmapa: manifest, audit, hybridní hledání) ---
# pyodbc            # vyžaduje systémový "ODBC Driver 18 for SQL Server"
